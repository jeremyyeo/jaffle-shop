models:
  - name: metricflow_time_spine
    description: Daily time spine for MetricFlow and the Semantic Layer.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: Base daily date column for MetricFlow joins.
        granularity: day
